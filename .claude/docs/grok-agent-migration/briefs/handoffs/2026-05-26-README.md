# Article Handoff — 2026-05-26

**Selected Brief**: "Most Agentic AI Projects Are Already Failing"
**Slug**: agentic-ai-is-failing-in-production
**Category**: Analysis
**Source Run**: Grok 4.3 article-idea-generator parallel run (2026-05-26)

## File
- Full brief: `2026-05-26-agentic-ai-is-failing-in-production.json`

## How to Invoke the Article-Writer (Advisor Pattern)

In Claude Code:

1. Use the `article-writer` agent:
   ```
   /agent article-writer
   ```

2. Paste the entire contents of the JSON file above as the input brief.

3. The agent (Opus) will return a raw JSON array of 4–5 paragraph strings.

4. **Before committing**:
   - JSON-validate the output.
   - Run it through `python3 -c "import json,sys; json.load(sys.stdin)"` or similar.
   - Use `scripts/add_article.py` to insert it deterministically into `data/articles.json`.
   - Update `data/site.json → lastUpdated`.
   - Run `npx jest`.
   - Then build and review.

## Voice Guidelines (already included in the brief)
- Direct, no hype. Lead with a strong claim or fact.
- Short paragraphs (~4-5 sentences). No bullet lists in article body.
- End with a forward-looking implication, not a summary.
- Skeptical but fair — acknowledge trade-offs.
- Audience: developers and technical founders.
- Target: 4–5 paragraphs, ~350–500 words total.

## Next Steps After Writing
- Validate output against the brief (key facts must be respected, no new invented claims).
- Insert via `add_article.py`.
- Update memory in `.claude/agent-memory/article-idea-generator.md` (mark this brief as published/absorbed).
- Proceed with the rest of the weekly update.

This handoff was prepared after the Grok parallel idea generation run.